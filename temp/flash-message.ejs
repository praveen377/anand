

<% if (locals.message) { %>
<div class="messages">
  <ul class="messages">
    <% Object.keys(message).forEach((key) => { %> 
      <% message[key].forEach((message) => { %>
    <li class="<%= key %>"><%= message %></li>
    <% }) %>
   <% }) %>
  </ul>
</div>
<% } %>
